query blogs($pageIndex: Int!, $pageSize: Int!){
  blogs(pageIndex: $pageIndex, pageSize: $pageSize) {
    list {
      _id
      title
      createDate
      author {
        nickname
      }
      category {
        text
      }
    }
    pagination {
      total
      pageIndex
      pageSize
    }
  }
}